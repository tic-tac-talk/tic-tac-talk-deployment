apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: prod
  labels:
    app: gateway-service
    env: prod
spec:
  type: NodePort
  selector:
    app: gateway-service
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
