apiVersion: v1
kind: Service
metadata:
  name: voice-service
  namespace: prod
  labels:
    app: voice-service
    env: prod
spec:
  type: ClusterIP
  selector:
    app: voice-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
