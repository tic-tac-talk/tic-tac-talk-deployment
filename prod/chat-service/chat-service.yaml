apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: prod
  labels:
    app: chat-service
    env: prod
spec:
  type: ClusterIP
  selector:
    app: chat-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
