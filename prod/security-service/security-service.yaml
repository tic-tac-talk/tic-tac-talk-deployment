apiVersion: v1
kind: Service
metadata:
  name: security-service
  namespace: prod
  labels:
    app: security-service
    env: prod
spec:
  type: ClusterIP
  selector:
    app: security-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
