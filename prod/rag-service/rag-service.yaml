apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: prod
  labels:
    app: rag-service
    env: prod
spec:
  type: ClusterIP
  selector:
    app: rag-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
